# 파일 편집 (File Editing)

OpenManus는 강력한 파일 편집 도구를 통해 텍스트 파일을 자동으로 수정, 생성, 관리할 수 있습니다. 코드 파일부터 설정 파일, 문서까지 다양한 형식의 파일을 프로그래밍 방식으로 처리할 수 있습니다.

## 개요

파일 편집 기능의 핵심 요소:

- **정확한 문자열 치환**: str_replace를 통한 정밀한 편집
- **다중 파일 편집**: 여러 파일을 일괄 처리
- **백업 및 복원**: 안전한 편집을 위한 백업 기능
- **패턴 기반 수정**: 정규표현식을 활용한 고급 편집
- **코드 리팩토링**: 구조적 코드 변경

:::info[📖 파일 편집 자동화]

**정의**: 파일 편집 자동화는 텍스트 파일의 내용을 프로그래밍 방식으로 읽고, 수정하고, 저장하는 과정을 자동화하는 것입니다.

**쉬운 비유**:
워드프로세서에서 "찾기 및 바꾸기" 기능을 사용하는 것과 비슷하지만, 훨씬 더 강력합니다. 수십 개의 파일에서 복잡한 패턴을 찾아 일관되게 수정하고, 조건에 따라 다르게 처리할 수 있습니다.

**메커니즘**:
```
┌─────────────────────────────────┐
│ 1. 파일 읽기                    │
│    - 원본 내용 로드             │
│    - 인코딩 감지               │
└─────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ 2. 내용 분석                    │
│    - 패턴 매칭                 │
│    - 컨텍스트 이해             │
└─────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ 3. 변경 적용                    │
│    - 문자열 치환               │
│    - 라인 추가/삭제            │
│    - 구조 변경                 │
└─────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ 4. 파일 저장                    │
│    - 백업 생성 (선택)          │
│    - 변경사항 기록             │
│    - 무결성 검증               │
└─────────────────────────────────┘
```

**존재 이유**:
1. **일관성**: 여러 파일에 동일한 변경사항을 정확하게 적용
2. **효율성**: 수동 편집보다 훨씬 빠르고 정확
3. **재현성**: 동일한 편집을 언제든 반복 가능
4. **안전성**: 백업과 검증을 통한 안전한 수정

:::

## 기본 사용법

### 문자열 치환

```python
from app.agent.manus import Manus

agent = await Manus.create()

# 간단한 문자열 치환
await agent.run("""
config.py 파일에서
'DEBUG = False'를 'DEBUG = True'로 변경해줘.
""")

# 여러 곳 치환
await agent.run("""
app.py 파일에서 모든 'print('를 'logger.info('로 변경해줘.
""")
```

### 라인 추가 및 삭제

```python
# 파일 끝에 추가
await agent.run("""
requirements.txt 파일 끝에
'pandas>=2.0.0'
'numpy>=1.24.0'
두 줄을 추가해줘.
""")

# 특정 라인 삭제
await agent.run("""
config.yaml에서
'# deprecated setting' 주석이 있는 라인과
그 다음 라인을 삭제해줘.
""")
```

### 섹션 수정

```python
# 함수 전체 교체
await agent.run("""
utils.py에서 calculate_tax 함수를 다음으로 교체해줘:

def calculate_tax(amount, rate=0.1):
    '''세금 계산'''
    if amount < 0:
        raise ValueError("금액은 0 이상이어야 합니다")
    return amount * rate
""")
```

## 고급 편집 기법

### 조건부 편집

```python
# 특정 조건에서만 수정
await agent.run("""
모든 .py 파일에서:
- 'TODO' 주석이 있는 라인을 찾아서
- 해당 TODO가 완료되었다면 주석 제거
- 미완료라면 '# PRIORITY:' 접두사 추가
""")
```

### 패턴 기반 수정

```python
# 정규표현식 사용
await agent.run("""
모든 Python 파일에서:
1. 'def function_name(' 패턴을 찾아서
2. 타입 힌트가 없으면 추가
3. docstring이 없으면 추가
""")
```

### 구조적 변경

```python
# 코드 리팩토링
await agent.run("""
legacy_code.py를:
1. 전역 변수를 클래스 속성으로 변환
2. 함수를 클래스 메서드로 변환
3. 하드코딩된 값을 상수로 추출
4. 결과를 refactored_code.py로 저장
""")
```

## 다중 파일 편집

### 일괄 치환

```python
await agent.run("""
src/ 디렉토리의 모든 JavaScript 파일에서:
- 'var'를 'const'로 변경
- 'function'을 화살표 함수로 변경
- 변경된 파일 목록 출력
""")
```

### 프로젝트 전체 리네이밍

```python
await agent.run("""
프로젝트 전체에서 'OldClassName'을 'NewClassName'으로 변경:
1. 모든 .py 파일 검색
2. 클래스 정의 및 사용처 모두 변경
3. import 문도 업데이트
4. 변경 사항 리포트 생성
""")
```

### 설정 파일 일괄 수정

```python
await agent.run("""
모든 config.yaml 파일에서:
- 'version: 1.0'을 'version: 2.0'으로 변경
- 새로운 설정 'timeout: 30' 추가
- 변경된 파일 경로 목록 저장
""")
```

## 안전한 편집

### 백업 생성

```python
# 편집 전 백업
await agent.run("""
important_file.py를 수정하기 전에:
1. important_file.py.bak으로 백업
2. 수정 작업 수행
3. 성공하면 백업 유지, 실패하면 복원
""")
```

### 변경사항 검증

```python
# 수정 후 검증
await agent.run("""
config.json을 수정한 후:
1. JSON 형식이 유효한지 검증
2. 필수 키가 모두 있는지 확인
3. 값의 타입이 올바른지 검증
문제가 있으면 원래대로 복원
""")
```

### Diff 생성

```python
# 변경사항 확인
await agent.run("""
파일 수정 후 git diff 형식으로
변경사항을 보여주고 changes.diff 파일로 저장해줘.
""")
```

## 실전 예제

### 코드 스타일 통일

```python
await agent.run("""
Python 프로젝트 코드 스타일 통일:

대상 파일: src/**/*.py

작업:
1. 들여쓰기를 4칸 스페이스로 통일
2. 라인 길이 88자 제한 (Black 스타일)
3. import 문 정렬 (isort)
4. 타입 힌트 추가 (가능한 경우)
5. docstring 형식 통일

결과물:
- 수정된 파일 목록
- 스타일 위반 리포트
- 자동 수정 불가능한 항목 리스트
""")
```

### API 버전 업그레이드

```python
await agent.run("""
API v1에서 v2로 마이그레이션:

파일: api/**/*.py

변경사항:
1. 엔드포인트 URL 업데이트
   - /api/v1/ → /api/v2/
2. 함수 시그니처 변경
   - get_user(id) → get_user(user_id)
3. 응답 형식 변경
   - {'status': 'ok'} → {'success': True}
4. deprecated 함수 제거

결과:
- 마이그레이션 리포트
- 수동 검토 필요 항목
- 테스트 업데이트 가이드
""")
```

### 문서 자동 업데이트

```python
await agent.run("""
README 파일 자동 업데이트:

파일: README.md, docs/**/*.md

업데이트 내용:
1. 버전 번호 업데이트 (1.0.0 → 1.1.0)
2. 설치 명령어 업데이트
3. 새 기능 섹션 추가
4. 깨진 링크 수정
5. 코드 예제 최신화

결과:
- 업데이트된 문서 목록
- 링크 검증 리포트
""")
```

### 환경 설정 동기화

```python
await agent.run("""
개발/스테이징/프로덕션 환경 설정 동기화:

파일:
- .env.development
- .env.staging
- .env.production

작업:
1. 공통 설정 항목 추출
2. 환경별 차이점 확인
3. 누락된 설정 추가
4. 사용하지 않는 설정 제거
5. 설정 값 검증

결과:
- 동기화 리포트
- 환경별 차이점 문서
- 설정 가이드
""")
```

## 모범 사례

### 1. 명시적인 매칭

```python
# ❌ 모호한 패턴
await agent.run("'user'를 'customer'로 변경")

# ✅ 정확한 패턴
await agent.run("""
변수명 'user_id'를 'customer_id'로 변경하되,
'user_name'은 변경하지 말 것
""")
```

### 2. 컨텍스트 고려

```python
# ✅ 주변 코드 확인
await agent.run("""
함수 정의에서만 'async def'를 추가하고,
함수 호출에서는 변경하지 말 것
""")
```

### 3. 점진적 변경

```python
# ✅ 단계별 수정
await agent.run("""
리팩토링을 3단계로 나누어 수행:
1단계: 변수명 변경
2단계: 함수 분리
3단계: 클래스 구조 개선
각 단계 후 테스트 실행
""")
```

### 4. 문서화

```python
# ✅ 변경사항 기록
await agent.run("""
파일 수정 후 CHANGELOG.md에:
- 변경된 파일 목록
- 변경 이유
- 영향받는 기능
추가
""")
```

## 문제 해결

### 인코딩 문제

```python
# UTF-8이 아닌 파일 처리
await agent.run("""
legacy.txt 파일을:
1. EUC-KR 인코딩으로 읽기
2. 내용 수정
3. UTF-8로 다시 저장
""")
```

### 대용량 파일

```python
# 메모리 효율적 처리
await agent.run("""
100MB 로그 파일에서:
1. 라인별로 스트리밍 처리
2. 'ERROR' 패턴만 추출
3. 새 파일로 저장
메모리에 전체 파일을 로드하지 말 것
""")
```

### 복잡한 패턴

```python
# 여러 단계로 분리
await agent.run("""
복잡한 리팩토링을 단순 작업으로 분리:
1. 먼저 모든 TODO 주석 찾기
2. 각 TODO를 issue로 변환
3. 완료된 TODO 제거
4. 미완료 TODO에 우선순위 추가
""")
```

## 도구 통합

### Git과 연동

```python
await agent.run("""
파일 수정 후:
1. git status로 변경사항 확인
2. git add로 스테이징
3. 의미있는 커밋 메시지 생성
4. git commit 실행
""")
```

### 린터와 포매터

```python
await agent.run("""
코드 수정 후:
1. black으로 포매팅
2. isort로 import 정렬
3. flake8로 검사
4. 오류가 있으면 자동 수정
""")
```

### 테스트 자동화

```python
await agent.run("""
코드 변경 후:
1. 관련 테스트 파일 찾기
2. 테스트 실행
3. 실패하면 변경 롤백
4. 성공하면 커밋
""")
```

## 다음 단계

파일 편집 기능에 대해 이해했다면:

- [도구 (Tools)](../concepts/tools.mdx) - StrReplaceEditor 도구 상세 정보
- [가이드: 코드 리팩토링](../guides/code-refactoring.mdx) - 체계적인 리팩토링 방법
- [가이드: 프로젝트 마이그레이션](../guides/project-migration.mdx) - 대규모 코드베이스 업데이트
