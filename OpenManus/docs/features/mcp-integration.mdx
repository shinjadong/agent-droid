# MCP 통합 (MCP Integration)

OpenManus는 [Model Context Protocol (MCP)](https://modelcontextprotocol.io)을 통해 외부 도구와 서비스를 에이전트에 통합할 수 있습니다. MCP는 AI 애플리케이션과 데이터 소스 간의 표준화된 연결 방식을 제공합니다.

## 개요

MCP 통합을 통해 다음과 같은 기능을 사용할 수 있습니다:

- **외부 도구 연결**: MCP 서버를 통한 다양한 외부 도구 사용
- **표준화된 인터페이스**: 일관된 방식으로 도구 호출
- **동적 도구 로딩**: 런타임에 MCP 도구 추가/제거
- **다중 서버 지원**: 여러 MCP 서버 동시 사용
- **자동 스키마 변환**: MCP 스키마를 OpenManus 도구로 자동 변환

:::info[📖 Model Context Protocol (MCP)]

**정의**: MCP는 AI 애플리케이션이 외부 데이터 소스, 도구, 서비스와 상호작용할 수 있도록 하는 개방형 프로토콜입니다.

**쉬운 비유**:
MCP는 전기 콘센트의 표준 규격과 같습니다. 어떤 전자기기든 표준 플러그만 있으면 콘센트에 연결할 수 있는 것처럼, MCP를 따르는 모든 도구는 OpenManus에 쉽게 연결할 수 있습니다. 각 제조사마다 다른 플러그를 만드는 대신, 하나의 표준으로 통일한 것입니다.

**메커니즘**:
```
┌─────────────────────────────────────────┐
│ OpenManus Agent                         │
│  ┌──────────────────────────────────┐   │
│  │ MCP Client                       │   │
│  │  - 도구 목록 요청               │   │
│  │  - 도구 호출                    │   │
│  │  - 결과 수신                    │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
           │ MCP Protocol
           ▼
┌─────────────────────────────────────────┐
│ MCP Server                              │
│  ┌──────────────────────────────────┐   │
│  │ Tools                            │   │
│  │  - weather: 날씨 정보           │   │
│  │  - database: DB 조회            │   │
│  │  - api: 외부 API 호출           │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

**존재 이유**:
1. **표준화**: 모든 도구가 같은 방식으로 작동
2. **확장성**: 새로운 도구를 쉽게 추가
3. **재사용성**: 한 번 만든 MCP 도구를 여러 곳에서 사용
4. **상호운용성**: 다른 MCP 호환 시스템과 도구 공유

:::

## 설정

### MCP 서버 설정

`config.yaml`에서 MCP 서버를 설정합니다:

```yaml
mcp:
  servers:
    - name: "filesystem"
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-filesystem", "/workspace"]
      
    - name: "weather"
      command: "python"
      args: ["-m", "mcp_server_weather"]
      env:
        WEATHER_API_KEY: "your-api-key"
        
    - name: "database"
      command: "uvx"
      args: ["mcp-server-postgres"]
      env:
        DATABASE_URL: "postgresql://localhost/mydb"
```

### 환경 변수

```bash
# MCP 설정 파일 경로 (선택사항)
export MCP_CONFIG_PATH=/path/to/mcp-config.json

# MCP 서버별 환경 변수
export WEATHER_API_KEY=your-api-key
export DATABASE_URL=postgresql://localhost/mydb
```

## 사용 방법

### MCP 에이전트 사용

MCP 통합이 활성화된 에이전트를 사용하는 방법:

```python
from app.agent.mcp import MCPAgent

async def main():
    # MCP 에이전트 생성
    agent = await MCPAgent.create()
    
    try:
        # MCP 도구를 사용하여 작업 수행
        result = await agent.run("현재 서울의 날씨를 알려줘")
        print(result)
        
        # 여러 MCP 서버의 도구를 조합
        result = await agent.run(
            "데이터베이스에서 최근 판매 데이터를 가져와서 분석하고, "
            "결과를 파일로 저장해줘"
        )
        print(result)
        
    finally:
        await agent.cleanup()
```

### 명령줄에서 실행

```bash
# MCP 모드로 에이전트 실행
python run_mcp.py --prompt "날씨 정보를 확인하고 보고서 작성해줘"

# stdio 모드 (표준 입출력)
python run_mcp.py --stdio

# SSE 모드 (Server-Sent Events)
python run_mcp.py --sse --port 8000
```

## MCP 도구 활용

### 파일 시스템 도구

```python
# config.yaml에 filesystem MCP 서버 설정 후

await agent.run("""
workspace 디렉토리의 모든 Python 파일을 찾아서,
각 파일의 크기와 마지막 수정 시간을 포함한 목록을 만들어줘.
""")

await agent.run("""
config.json 파일의 내용을 읽어서,
중요한 설정값들을 분석하고 문제가 있는지 확인해줘.
""")
```

### 데이터베이스 도구

```python
# config.yaml에 database MCP 서버 설정 후

await agent.run("""
users 테이블에서 최근 30일간 가입한 사용자 수를 조회하고,
일별 가입 추이 그래프를 생성해줘.
""")

await agent.run("""
orders 테이블에서 오늘의 주문 데이터를 가져와서,
제품별 판매량과 매출을 집계해줘.
""")
```

### API 통합 도구

```python
# 날씨 API 예제
await agent.run("""
서울, 부산, 제주도의 현재 날씨를 확인하고,
각 지역의 기온과 날씨 상태를 비교 분석해줘.
""")

# 번역 API 예제
await agent.run("""
report.txt 파일의 내용을 읽어서 영어로 번역하고,
translated_report.txt로 저장해줘.
""")
```

## 커스텀 MCP 서버 만들기

### Python MCP 서버 예제

```python
# my_mcp_server.py
from mcp.server import Server
from mcp.types import Tool, TextContent

server = Server("my-custom-server")

@server.list_tools()
async def list_tools() -> list[Tool]:
    """사용 가능한 도구 목록 반환"""
    return [
        Tool(
            name="calculate_tax",
            description="주어진 금액의 세금을 계산합니다",
            inputSchema={
                "type": "object",
                "properties": {
                    "amount": {
                        "type": "number",
                        "description": "세금을 계산할 금액"
                    },
                    "rate": {
                        "type": "number",
                        "description": "세율 (퍼센트)",
                        "default": 10
                    }
                },
                "required": ["amount"]
            }
        ),
        Tool(
            name="format_currency",
            description="숫자를 통화 형식으로 포맷팅합니다",
            inputSchema={
                "type": "object",
                "properties": {
                    "amount": {"type": "number"},
                    "currency": {
                        "type": "string",
                        "enum": ["KRW", "USD", "EUR"],
                        "default": "KRW"
                    }
                },
                "required": ["amount"]
            }
        )
    ]

@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    """도구 실행"""
    if name == "calculate_tax":
        amount = arguments["amount"]
        rate = arguments.get("rate", 10)
        tax = amount * (rate / 100)
        
        return [TextContent(
            type="text",
            text=f"금액: {amount:,.0f}원\n세율: {rate}%\n세금: {tax:,.0f}원\n총액: {amount + tax:,.0f}원"
        )]
        
    elif name == "format_currency":
        amount = arguments["amount"]
        currency = arguments.get("currency", "KRW")
        
        if currency == "KRW":
            formatted = f"₩{amount:,.0f}"
        elif currency == "USD":
            formatted = f"${amount:,.2f}"
        elif currency == "EUR":
            formatted = f"€{amount:,.2f}"
        else:
            formatted = str(amount)
            
        return [TextContent(
            type="text",
            text=formatted
        )]
    
    raise ValueError(f"Unknown tool: {name}")

# 서버 실행
if __name__ == "__main__":
    import asyncio
    asyncio.run(server.run())
```

### MCP 서버 등록

```yaml
# config.yaml에 추가
mcp:
  servers:
    - name: "my-custom-server"
      command: "python"
      args: ["my_mcp_server.py"]
```

### 커스텀 서버 사용

```python
await agent.run("""
100,000원의 세금을 15% 세율로 계산하고,
결과를 한국 원화 형식으로 포맷팅해줘.
""")
# 출력:
# 금액: 100,000원
# 세율: 15%
# 세금: 15,000원
# 총액: ₩115,000
```

## 고급 기능

### 도구 필터링

특정 MCP 도구만 사용하도록 제한할 수 있습니다:

```python
from app.agent.mcp import MCPAgent

agent = await MCPAgent.create(
    allowed_tools=["calculate_tax", "format_currency"]  # 특정 도구만 허용
)

# 또는 특정 도구 제외
agent = await MCPAgent.create(
    blocked_tools=["dangerous_operation"]  # 위험한 도구 차단
)
```

### 도구 체이닝

여러 MCP 도구를 연쇄적으로 사용:

```python
await agent.run("""
1. 데이터베이스에서 이번 달 매출 데이터 조회
2. Python으로 데이터 분석 및 시각화
3. 분석 결과를 파일로 저장
4. 파일의 내용을 읽어서 요약 보고서 작성
""")
```

### 리소스 관리

MCP 서버는 파일, 데이터베이스 연결 등의 리소스를 제공할 수 있습니다:

```python
# MCP 서버에서 제공하는 리소스 목록 확인
async def list_resources():
    """사용 가능한 MCP 리소스 목록"""
    resources = await agent.mcp_client.list_resources()
    for resource in resources:
        print(f"- {resource.name}: {resource.description}")
        print(f"  URI: {resource.uri}")
```

### 프롬프트 템플릿

MCP 서버는 사전 정의된 프롬프트를 제공할 수 있습니다:

```python
# MCP 서버의 프롬프트 사용
await agent.run("""
'daily_report' 프롬프트를 사용해서 오늘의 업무 보고서를 작성해줘.
""")
```

## 모범 사례

### 1. 오류 처리

```python
async def safe_mcp_call():
    """MCP 호출 안전하게 처리"""
    try:
        result = await agent.run("MCP 도구를 사용한 작업")
        return result
    except MCPConnectionError as e:
        print(f"MCP 서버 연결 실패: {e}")
        # 폴백 로직
        return await agent.run("MCP 없이 작업 수행")
    except MCPToolError as e:
        print(f"MCP 도구 실행 실패: {e}")
        # 대체 도구 사용
        return await agent.run("다른 도구로 재시도")
```

### 2. 성능 최적화

```python
# ❌ 비효율적: 여러 번 에이전트 실행
result1 = await agent.run("데이터베이스에서 데이터 조회")
result2 = await agent.run("데이터 분석")
result3 = await agent.run("결과 저장")

# ✅ 효율적: 한 번에 요청
result = await agent.run("""
1. 데이터베이스에서 데이터 조회
2. 데이터 분석
3. 결과 저장
""")
```

### 3. 보안 고려사항

```python
# 민감한 정보는 환경 변수로 관리
import os

# ❌ 하드코딩하지 않기
agent_config = {
    "mcp": {
        "servers": [{
            "env": {
                "API_KEY": "sk-1234567890"  # 위험!
            }
        }]
    }
}

# ✅ 환경 변수 사용
agent_config = {
    "mcp": {
        "servers": [{
            "env": {
                "API_KEY": os.getenv("API_KEY")  # 안전
            }
        }]
    }
}
```

### 4. 테스트

```python
import pytest
from unittest.mock import Mock, patch

@pytest.mark.asyncio
async def test_mcp_tool_call():
    """MCP 도구 호출 테스트"""
    # MCP 클라이언트 모킹
    with patch('app.agent.mcp.MCPClient') as mock_client:
        mock_client.call_tool.return_value = {
            "result": "성공"
        }
        
        agent = await MCPAgent.create()
        result = await agent.run("테스트 작업")
        
        assert "성공" in result
        mock_client.call_tool.assert_called_once()
```

## 문제 해결

### 일반적인 문제

#### 1. MCP 서버 연결 실패

```bash
# 문제: MCP 서버가 시작되지 않음
Error: Failed to connect to MCP server 'filesystem'

# 해결:
# 1. MCP 서버 패키지 설치 확인
npm list -g @modelcontextprotocol/server-filesystem

# 2. 명령어 경로 확인
which npx

# 3. 권한 확인
chmod +x /path/to/mcp-server
```

#### 2. 도구 호출 오류

```bash
# 문제: 도구를 찾을 수 없음
Error: Tool 'calculate_tax' not found

# 해결:
# 1. 도구 목록 확인
await agent.list_available_tools()

# 2. MCP 서버 재시작
await agent.restart_mcp_servers()

# 3. 설정 파일 검증
await agent.validate_mcp_config()
```

#### 3. 스키마 불일치

```bash
# 문제: 잘못된 인자 전달
Error: Invalid arguments for tool 'calculate_tax'

# 해결:
# 1. 도구 스키마 확인
tool_schema = await agent.get_tool_schema("calculate_tax")
print(tool_schema)

# 2. 필수 인자 확인
# inputSchema의 'required' 필드 확인
```

## 다음 단계

MCP 통합에 대해 이해했다면:

- [브라우저 자동화](./browser-automation.mdx) - Playwright를 통한 웹 자동화
- [데이터 분석](./data-analysis.mdx) - 데이터 처리 및 분석 기능
- [가이드: MCP 서버 개발](../guides/developing-mcp-servers.mdx) - 커스텀 MCP 서버 만들기
